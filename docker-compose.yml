services:

  redis:
    image: redis:latest
    container_name: rate-limiter-redis
    ports:
      - "6379:6379"

  app:
    build: .
    container_name: rate-limiter-app
    ports:
      - "8081:8081"
    depends_on:
      - redis